spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d9tbppid0e5s738pnc90-a:5432/aven_8v5s}
    username: ${SPRING_DATASOURCE_USERNAME:${DB_USER:aven_admin}}
    password: ${SPRING_DATASOURCE_PASSWORD:${DB_PASSWORD:3ZeXstHHTrewTf1vxK67SkHjPORt2PXD}}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    open-in-view: false

  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 1.0
    locations: classpath:db/migration

jwt:
  secret-key: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION:86400000}

app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:${CORS_ALLOWED_ORIGINS:https://aven.asenterprises.com}}

logging:
  level:
    root: WARN
    com.asenterprises.bms: INFO
